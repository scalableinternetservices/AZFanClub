<%= form_for([@poll, @user, @user.comments.build]) do |f| %>
  


  <div class="field">
    <%= f.label "Comment" %><br />
    <%= f.text_field :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>