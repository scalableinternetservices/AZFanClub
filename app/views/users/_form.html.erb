<%= form_for([@poll, @poll.users.build]) do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <%= render 'time_frames/form',
            :locals => {:form => f} %>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>